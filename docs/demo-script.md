# Demo Video Script (5-7 min)

1. Open project and explain folder structure.
2. Show `.env` and switch `TEST_ENV`.
3. Run `npm run test:ui` and inspect passing tests.
4. Intentionally break credentials to show failure screenshot/trace/video.
5. Run `npm run test:api` and show CRUD checks.
6. Generate and open Allure report.
7. Show GitHub Actions pipeline YAML and Docker execution command.
